## Improvements

- Center the cover art in the left pane horizontally so that it is perfectly centered.
- Remove the inlined description in the lists.
- Enforce a standard size for cover images and resync the ones that have small cover images.
- Ensure that the downloaded track's meta is properly updated and that we don't assign incorrect data to the track. Also ensure that the cover tarck is downloaded immediately as well.
- Fix the visualizer and progress bar presets to update the widget as the user scrolls the list and show the previews at the bottom panes.
- Readd help text for the meta editing floating window and also fix the current bug that makes it impossible to input text into the track fields.
- When the download fails for the first time, automatically retry it up to 3 times and only warn the user when it fails on the 3rd attempt.
- Fix the bug that shows the total track count in the Liked list when we are navigating the left pane.
- Make Alt+V and Alt+P to cycle through the visualizer and progress bar presets.
- When the view height is too small don't render the track info.
- Ensure that the spotify integration actually works as expected

Create one commit for all these changes and respect the terse commit style. Create a spec for each related set of tasks.
But first, fix the error that caused publishing to crates.io fail, fix it and retrigger the workflow
